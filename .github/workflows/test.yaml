name: "Test Plugin"

on:
  push:
    branches:
      - master
    paths-ignore:
      - "**/*.md"
  pull_request:
    branches:
      - master
    paths-ignore:
      - "**/*.md"
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/test.yaml@2d1d8651c878e11ce83de5ecdbedb31e70ebc6f0 # master
    with:
      dotnet-version: "9.0.x"
